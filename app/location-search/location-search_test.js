'use strict';

describe('getCrafty.locationSearch module', function() {

  beforeEach(module('getCrafty.locationSearch'));

  describe('locationSearch controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var locationCtrl = $controller('locationSearchCtrl');
      expect(locationCtrl).toBeDefined();
    }));

  });
});