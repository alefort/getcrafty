'use strict';

describe('getCrafty.storeSearch module', function() {

  beforeEach(module('getCrafty.storeSearch'));

  describe('storeSearch controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var storeCtrl = $controller('storeSearchCtrl');
      expect(storeCtrl).toBeDefined();
    }));

  });
});