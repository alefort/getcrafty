'use strict';

describe('getCrafty.beerSearch module', function() {

  beforeEach(module('getCrafty.beerSearch'));

  describe('beerSearch controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var beerCtrl = $controller('beerSearchCtrl');
      expect(beerCtrl).toBeDefined();
    }));

  });
});