'use strict';

describe('getCrafty.stores module', function() {

  beforeEach(module('getCrafty.stores'));

  describe('stores controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var storeCtrl = $controller('storesCtrl');
      expect(storeCtrl).toBeDefined();
    }));

  });
});