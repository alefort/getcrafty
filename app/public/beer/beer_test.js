'use strict';

describe('getCrafty.beer module', function() {

  beforeEach(module('getCrafty.beer'));

  describe('beer controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var beerCtrl = $controller('beerCtrl');
      expect(beerCtrl).toBeDefined();
    }));

  });
});